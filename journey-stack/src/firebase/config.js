/**
 * firebase/config.js — stub
 *
 * Firebase has been fully removed from this project.
 * All auth, data, and storage now go through the Node.js/Express backend
 * backed by PostgreSQL and Cloudinary.
 *
 * This file is kept as a stub so any stale import doesn't crash the build,
 * but it can be safely deleted once all references are cleaned up.
 */
export const auth     = null;
export const db       = null;
export const storage  = null;
export const analytics = null;